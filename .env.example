# Public site URL used for SEO metadata (Open Graph, canonical links,
# robots.txt, sitemap.xml). Must be set to the real production URL when
# deploying; falls back to http://localhost:3000 in development.
NEXT_PUBLIC_SITE_URL=http://localhost:3000
